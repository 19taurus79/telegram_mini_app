# Project Rules

## TypeScript Code Quality
- **Запрет на `any`**: Никогда не использовать тип `any` в TypeScript коде проекта. Всегда объявлять точные интерфейсы, типы или при необходимости использовать `unknown`.
